{
  "name": "@dsp/create-package-mono",
  "version": "0.0.1",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "start:dev": "nodemon",
    "build": "yarn compile:pre && yarn compile && yarn compile:post",
    "build:clean": "yarn clean && yarn build",
    "compile:pre": "echo '[script-not-implemented]: compile:pre'",
    "compile": "tsc",
    "compile:post": "copyfiles bin/* dist",
    "clean": "rimraf ./dist || echo Skipping 'clean' step due to error",
    "start": "yarn build && node build/index.js"
  },
  "bin": {
    "cpm": "src/index.ts"
  },
  "keywords": [],
  "author": "dplantera",
  "license": "ISC",
  "dependencies": {
    "commander": "^9.4.0",
    "copyfiles": "^2.4.1"
  },
  "peerDependencies": {
    "ts-node": "^10.9.1"
  }
}
